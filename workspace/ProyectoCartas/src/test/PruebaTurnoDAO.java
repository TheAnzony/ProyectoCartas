package test;

import java.util.List;

import dao.TurnoDAO;
import modulos.Turno;

public class PruebaTurnoDAO {

	public static void main(String[] args) {

		/*TurnoDAO dao = new TurnoDAO();

		// ── 1. INSERTAR ──────────────────────────────────────────────────────────
		// Requiere que exista una partida con id=1 y un jugador con id=1 en la BD
		System.out.println("=== INSERTAR ===");
		 Turno turno = new Turno(0, 1, 1, 1, 3, 0, 100, 100);
		boolean insertado = dao.insertar(turno);
		System.out.println("Resultado insertar: " + insertado);

		// ── 2. LISTAR TODOS ──────────────────────────────────────────────────────
		System.out.println("\n=== LISTAR TODOS ===");
		List<Turno> todos = dao.listar();
		if (todos.isEmpty()) {
			System.out.println("No hay turnos en la base de datos.");
		} else {
			for (Turno t : todos) {
				System.out.println(t);
			}
		}

		// ── 3. LISTAR POR PARTIDA ────────────────────────────────────────────────
		System.out.println("\n=== LISTAR POR PARTIDA (id=1) ===");
		List<Turno> porPartida = dao.listarPorPartida(1);
		if (porPartida.isEmpty()) {
			System.out.println("No hay turnos para la partida 1.");
		} else {
			for (Turno t : porPartida) {
				System.out.println(t);
			}
		}

		// ── 4. ELIMINAR ──────────────────────────────────────────────────────────
		System.out.println("\n=== ELIMINAR (id=1) ===");
		boolean eliminado = dao.eliminar(1);*/

	}

}
